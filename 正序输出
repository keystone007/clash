#include<stdio.h>

int main(){
    int num;
        scanf("%d",&num);
        int a=num%100;
        int b=(num-100*a)%10;
        int c=num-100*a-10*b;
        printf("%d %d %d\n",a,b,c);
        return 0;
}
